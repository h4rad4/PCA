#include <stdio.h>

int main()
{
    int count = 0, num = 0, add = 0;

    while (count < 5) {
        printf("Type a number: ");
        scanf("%i", &num);
        count++;
        add += num;
    }

    printf("ADDITTION = %i", add);
    return 0;
}
